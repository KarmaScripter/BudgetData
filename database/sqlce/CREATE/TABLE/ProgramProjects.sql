CREATE TABLE IF NOT EXISTS "ProgramProjects" 
(
	"ProgramProjectId" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT UNIQUE,
	"Code"	TEXT NOT NULL DEFAULT NS,
	"Name"	TEXT NOT NULL DEFAULT NS,
	"Title"	TEXT NOT NULL DEFAULT NS,
	"Laws"	TEXT NOT NULL DEFAULT NS,
	"Narrative"	TEXT NOT NULL DEFAULT NS,
	"Definition"	TEXT NOT NULL DEFAULT NS,
	"ProgramAreaCode" TEXT NOT NULL DEFAULT NS,
	"ProgramAreaName" TEXT NOT NULL DEFAULT NS
);
