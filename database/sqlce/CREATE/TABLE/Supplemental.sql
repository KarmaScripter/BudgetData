CREATE TABLE IF NOT EXISTS "Supplemental" 
(
	"SupplementalId" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT UNIQUE,
	"Type"	TEXT NOT NULL DEFAULT NS,
	"RcCode" TEXT NOT NULL,
	"FundCode"	TEXT NOT NULL DEFAULT NS,
	"BFY" TEXT NOT NULL DEFAULT NS,
	"BocCode" TEXT NOT NULL DEFAULT NS,
	"BocName" TEXT NOT NULL DEFAULT NS,
	"Amount" FLOAT NOT NULL DEFAULT 0.0,
	"Time" TEXT NOT NULL DEFAULT NS
);
